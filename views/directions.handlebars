{{> header}}

	<div style="display:none" id="address">{{address}}</div>

	<div id="place-info" class="row">
		<div class="col-md-12 col-xs-12">
		<h1 id="place-name">
			{{place}}
		</h1>
		<!-- <a href="/places/{{category}}/{{uid}}">&lt; back to info</a> -->
	</div>
	</div>

	<div class="row">
		<div class="col-md-12 col-xs-12" id="directions">
		</div>
	</div>

	<div id="map-panel" class="row pd"></div>

	

{{> footer}}