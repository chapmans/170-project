{{> header}}

<h1>Suggest a mom and pop store!</h1>

<form role="form" id="suggest-store">
	<div class="form-group">
		<label for="name">Store Name:</label>
		<input type="text" class="form-control" name="suggest-store-name" placeholder="Name">
	</div>
	<div class="form-group">
		<label for="name">Category:</label>
		<select class="form-control">
	  	<option {{#if coffee }}selected{{/if}}>Coffee</option>
	  	<option {{#if desserts }}selected{{/if}}>Desserts</option>
	  	<option {{#if restaurants }}selected{{/if}}>Restaurants</option>
		  <option {{#if clothing }}selected{{/if}}>Clothing</option>
		  <option {{#if trinkets }}selected{{/if}}>Trinkets</option>
		  <option {{#if music }}selected{{/if}}>Music</option>
		</select>
	</div>
	<div class="form-group">
		<label for="name">Address:</label>
		<input type="text" class="form-control" name="suggest-store-name" placeholder="Address (Optional)">
	</div>
	<div class="form-group">
		<label for="name">Store Information:</label>
		<textarea class="form-control" name="suggest-store-name"></textarea>
	</div>
	<a type="submit" class="btn btn-default" id="suggest-submit" href="/suggest/thanks">Submit</a>
</form>

{{> footer}}